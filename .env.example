# ============================================================================
# ecom-agent-shared 配置示例
# 复制成 .env 后按需修改。只想离线跑通流程的话，用下面「离线模式」那三行即可。
# ============================================================================

# ---------------------------------------------------------------------------
# 离线模式（不需要任何 API Key，不联网）
# 用于：CI、本地演示、只验证编排链路。返回的内容带 [MOCK] 前缀，一眼可辨。
# ---------------------------------------------------------------------------
# LLM_PROVIDER=mock
# VECTOR_BACKEND=lexical

# ---------------------------------------------------------------------------
# 模型接入：所有厂商统一走 OpenAI 兼容协议，换厂商只改这一个变量
# 可选：dashscope（阿里云百炼）| deepseek | openai | ollama | mock
# ---------------------------------------------------------------------------
LLM_PROVIDER=dashscope
LLM_API_KEY=
# 留空则用该厂商的默认地址；自建网关 / 代理时填这里
# LLM_API_BASE=
# 留空则用该厂商的默认轻/重模型
# MODEL_LIGHT=
# MODEL_HEAVY=
# MODEL_EMBEDDING=
REQUEST_TIMEOUT=120

# ---------------------------------------------------------------------------
# 检索
# ---------------------------------------------------------------------------
# chroma（生产默认，需 pip install chromadb）
# numpy （零依赖暴力检索，需厂商提供 embedding）
# lexical（纯词法 BM25，不需要向量，离线/CI 用它）
VECTOR_BACKEND=chroma
VECTOR_COLLECTION=ecom_kb
VECTOR_DIR=
CHUNK_SIZE=600
CHUNK_OVERLAP=50
TOP_K=4
# 混合重排里语义分的权重：0.7 表示语义主导、词面纠偏
RERANK_ALPHA=0.7

# ---------------------------------------------------------------------------
# 记忆
# ---------------------------------------------------------------------------
SESSION_MAX_TURNS=10
LONGTERM_MEMORY=true

# ---------------------------------------------------------------------------
# 观测
# ---------------------------------------------------------------------------
LOG_LEVEL=INFO
